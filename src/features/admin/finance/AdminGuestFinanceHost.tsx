import { CanonicalGuestFinancePanel } from './CanonicalGuestFinancePanel';

/** Admin Finance → Guest Funds discovery host (canonical paginated read model). */
export function AdminGuestFinanceHost() {
  return <CanonicalGuestFinancePanel />;
}
